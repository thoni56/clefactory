#include <cgreen/mocks.h>

#include "dispatcher.h"


int dispatch_command(CXIndex index, FileTable fileTable, const char *command, DispatchTable *table) {
    return mock(command, fileTable, table);
}
