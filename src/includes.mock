#include <cgreen/mocks.h>

#include "includes.h"


const char *includes_help(void) { return (const char *)mock(); }

int includes_handler(CXIndex index, const char *arguments[]) {
    return mock(arguments);
}
