#include <cgreen/mocks.h>

#include "indexer.h"


const char *indexer_help(void) { return (const char *)mock(); }

int indexer_handler(CXIndex index, const char *arguments[]) {
    return mock(arguments);
}
