#include <cgreen/mocks.h>
#include <cjson/cJSON.h>

extern cJSON *jsonParse(const char *json_string) {
    return (cJSON *)mock(json_string);
}

extern cJSON *jsonGetObjectItem(cJSON *object, const char *elementName) {
    return (cJSON *)mock(object, elementName);
}

extern void jsonDelete(cJSON *object) {
    mock(object);
}
