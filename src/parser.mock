#include <cgreen/mocks.h>

#include "parser.h"


const char *parser_help(void) { return (const char *)mock(); }

int parser_handler(CXIndex index, FileTable fileTable, const char *arguments[]) {
    return mock(index, fileTable, arguments);
}
